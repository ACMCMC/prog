name: C CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v2
    #- name: configure
    #  run: ./configure
    - name: make
      run: for d in ./*/ ; do (make); done
    #- name: make check
    #  run: make check
    #- name: make distcheck
    #  run: make distcheck
